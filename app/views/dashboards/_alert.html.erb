<template id="my-template">
  <swal-title>
  Are you sure ?
  </swal-title>
  <swal-icon type="warning" color="red"></swal-icon>
  <swal-button type="confirm">
    <%= link_to "YES I'M SURE", bike_path(@bikes), class: "delete-link", data: {turbo_method: :delete} %>
  </swal-button>
  <swal-button type="cancel">
    CANCEL
  </swal-button>
  <swal-param name="allowEscapeKey" value="false" />
  <swal-param
    name="customClass"
    value='{ "popup": "my-popup" }' />
</template>

<template id="my-template2">
  <swal-title>
  Are you sure ?
  </swal-title>
  <swal-icon type="warning" color="red"></swal-icon>
  <swal-button type="confirm">
    <%= link_to "YES I'M SURE", booking_path(bookings_url), class: "delete-link", data: {turbo_method: :delete} %>
  </swal-button>
  <swal-button type="cancel">
    CANCEL
  </swal-button>
  <swal-param name="allowEscapeKey" value="false" />
  <swal-param
    name="customClass"
    value='{ "popup": "my-popup" }' />
</template>
