<div class="container mt-3">
  <h1 class="mb-3">All our Bikes (<%= @bikes.length %>)</h1>
  <div class="d-flex justify-content-between">
    <% @bikes.each do |bike| %>
      <div class="card shadow p-3 mb-5 bg-body rounded" style="width: 18rem;">
        <div class="flat-card-image" style=""></div>
        <div class="card-body">
          <h5 class="card-title"><%= bike.bike_type %></h5>
          <p class="card-text"><%= bike.brand %></p>
          <p class="card-text"><%= bike.address %></p>
          <p class="card-text"><%= bike.description %></p>
          <i class="fa-solid fa-euro-sign"><%= bike.price_per_day %></i>
        </div>
        <div class="card-body d-flex justify-content-evenly">
          <%= link_to "More info", bikes_path(@bikes), class: "card-link text-primary" %>
          <%= link_to "Edit", edit_bike_path(bike), class: "card-link text-warning" %>
          <%= link_to "Remove", bike_path(bike), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "card-link text-danger" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
